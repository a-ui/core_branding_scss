<a name="quarks" aria-label="Quarks"></a>
<div class="u-wrapper u-margin-top d-h2">
    <div class="u-container">
        <h2>Quarks</h2>
    </div>
</div>
<div class="u-wrapper">
    <div class="u-container">
        <div class="d">
            <div>
                <p>You can treat this part of the package as the API of our styling library. Include it on top of your custom stylesheet in order to have access to all defined variables, mixins and functions.</p>
            </div>
            <div>
                <pre class="a-pre a-pre--scrollable"><code>@import 'quarks';</code></pre>
            </div>
        </div>



        <a name="quarks-mixins" aria-label="Mixins"></a>
        <h3 class="u-margin-top-xl">Mixins</h3>
        <span class="d-file">quarks/quarks.mixins</span>
        <div class="d">
            <div>
                <p>More mixins are available, but the following are the more important ones.</p>
            </div>
        </div>
        <div class="d">
            <div>
                <p>Force an element to self-clear its children</p>
                <div class="m-alert d-alert">
                    <p>Note that you can also use the <code>.clearfix</code> class.</p>
                </div>
            </div>
            <div>
                <pre class="a-pre a-pre--scrollable"><code>.css {
  @include clearfix;
}</code></pre>
            </div>
        </div>
        <div class="d">
            <div>
                <p>Responsive mixins</p>
                <div class="m-alert d-alert">
                    <p>Note that working mobile first is encouraged, so try to limit the use of the <code>to</code> mixin.</p>
                </div>
            </div>
            <div>
                <pre class="a-pre a-pre--scrollable"><code>.css {
  @include at($screen) {
    /* Anything bigger than $screen */
  }

  @include to($screen-xs-max) {
    /* Anything smaller or equal than $screen */
  }
}</code></pre>
            </div>
        </div>



        <a name="quarks-functions" aria-label="Functions"></a>
        <h3 class="u-margin-top-xl">Functions</h3>
        <span class="d-file">quarks/quarks.functions</span>
        <div class="d">
            <div>
                <p>More functions are available, but the following are the more important ones.</p>
            </div>
        </div>
        <div class="d">
            <div>
                <p>Convert pixels to rem</p>
            </div>
            <div>
                <pre class="a-pre a-pre--scrollable"><code>.css {
  margin: rem(32px) / 16 * 1rem // margin: 2rem;
}</code></pre>
            </div>
        </div>
        <div class="d">
            <div>
                <p>Correctly handle z-indexes</p>
                <div class="m-alert m-alert d-alert">
                    <p>All z-index options: 'overlay': 10, 'header': 9, 'statusbar': 8, 'pane': 7, 'pane-overlay': 6, 'tooltip': 5, 'flyout': 4, 'datepicker': 3, 'top': 2, 'base': 1, 'auto': 0, 'cellar': -1.</p>
                </div>
            </div>
            <div>
                <pre class="a-pre a-pre--scrollable"><code>.css {
  z-index: layer('base'); // z-index: 1;
}</code></pre>
            </div>
        </div>



        <a name="quarks-colors" aria-label="Colors"></a>
        <h3 class="u-margin-top-xl">Colors</h3>
        <span class="d-file">quarks/quarks.colors</span>
        <div class="d">
            <div>
                <p>An overview of the complete color palette.</p>
                {# <ul class="u-list-unstyled d-list d-list__colors">
                    {% for color in COLORS %}
                        <li class="d-{{ color.name | escape | replace('$', '') }}">
                            <span class="d-color" style="background: {{ color.compiledValue }}" aria-hidden="true"></span>
                            <code>{{ color.name }}</code> <strong>{{ color.compiledValue }}</strong></small>
                        </li>
                    {% endfor %}
                </ul> #}
            </div>
        </div>



        <a name="quarks-properties" aria-label="Custom properties"></a>
        <h3 class="u-margin-top-xl">Custom properties</h3>
        <span class="d-file">quarks/quarks.properties</span>
        <div class="d">
            <div>
                <p>Starting with core branding v6 we make use of CSS custom properties. There's an important difference between using them in your stylesheet and overriding them, e.g. for your own custom theme:</p>
                <h4 class="u-margin-top">Overriding core branding values</h4>
                <p class="u-margin-top-xs">Here's an overview of all available <code>:root</code> CSS custom properties for creating a custom theme based on core branding.</p>
                <div class="d-p__variables u-margin-top-xs u-margin-bottom">
                    <pre class="a-pre a-pre--scrollable">@use '@a-ui/core/src/styles/quarks' as *;
@use 'sass:color';

:root {
    --brand-1: #{$endeavour-600};
    --brand-1-tint: #{$endeavour-700};
    --brand-1-contrast: #{$white};
    --brand-1-alt: #{$white};
    --brand-1-alt-tint: #{$endeavour-100};
    --brand-1-alt-contrast: #{$black};

    --brand-2: #{$monza-600};
    --brand-2-tint: #{$monza-700};
    --brand-2-contrast: #{$white};
    --brand-2-alt: #{$white};
    --brand-2-alt-tint: #{$monza-100};
    --brand-2-alt-contrast: #{$black};

    --brand-3: #{$endeavour-600};
    --brand-3-tint: #{$endeavour-700};
    --brand-3-contrast: #{$white};
    --brand-3-alt: #{$white};
    --brand-3-alt-tint: #{$endeavour-100};
    --brand-3-alt-contrast: #{$black};

    --state-info: #{$endeavour-600};
    --state-info-tint: #{$endeavour-700};
    --state-info-contrast: #{$white};
    --state-info-alt: #{$white};
    --state-info-alt-tint: #{$endeavour-100};
    --state-info-alt-contrast: #{$black};

    --state-success: #{$mantis-700};
    --state-success-tint: #{$mantis-800};
    --state-success-contrast: #{$white};
    --state-success-alt: #{$white};
    --state-success-alt-tint: #{$mantis-100};
    --state-success-alt-contrast: #{$black};

    --state-warning: #{$burnt-orange-600};
    --state-warning-tint: #{$burnt-orange-700};
    --state-warning-contrast: #{$white};
    --state-warning-alt: #{$white};
    --state-warning-alt-tint: #{$burnt-orange-100};
    --state-warning-alt-contrast: #{$black};

    --state-danger: #{$cinnabar-700};
    --state-danger-tint: #{$cinnabar-800};
    --state-danger-contrast: #{$white};
    --state-danger-alt: #{$white};
    --state-danger-alt-tint: #{$cinnabar-100};
    --state-danger-alt-contrast: #{$black};

    --state-disabled: #{$neutral-100};
    --state-disabled-tint: #{$neutral-200};
    --state-disabled-contrast: #{$neutral-400};

    --text-color: #{$black};
    --text-color-light: #{$neutral-400};
    --text-color-inverted: #{$white};

    --bg-color: #{$neutral-100};
    --bg-color-light: #{$white};
    --bg-color-inverted: #{$neutral-500};

    --border-color: #{$neutral-300};
    --border-color-light: #{$neutral-200};

    --overlay-color: #{rgba($black, .75)};
    --box-shadow-color: #{rgba($neutral-600, .12)};

    --link: #{$endeavour-600};
    --link-tint: #{$endeavour-700};
    --link-contrast: #{$white};
    --link-alt: #{$white};
    --link-alt-tint: #{$endeavour-100};
    --link-alt-contrast: #{$black};

    --state-selected: #{color.mix($endeavour-600, $white, 20%)};

    --spacer: 1.5rem;
    --spacer-3xl: 6rem;
    --spacer-xxl: 3.75rem;
    --spacer-xl: 3rem;
    --spacer-l: 2.25rem;
    --spacer-s: 1rem;
    --spacer-xs: .75rem;
    --spacer-xxs: .5rem;
    --spacer-3xs: .25rem;

    --screen-xs: #{$screen-xs};
    --screen-s: #{$screen-sm};
    --screen-l: #{$screen-md};
    --screen-xl: #{$screen-lg};
    --screen-xxl: #{$screen-xl};

    --screen-xxs-max: #{$screen-xxs-max};
    --screen-xs-max: #{$screen-xs-max};
    --screen-s-max: #{$screen-sm-max};
    --screen-l-max: #{$screen-md-max};
    --screen-xl-max: #{$screen-lg-max};

    --font-family-base: 'Sun Antwerpen', sans-serif;
    --font-family-headings: 'Antwerpen Regular', sans-serif;
    --font-family-monospace: 'Menlo', 'Monaco', 'Consolas', 'Courier New', monospace;

    --font-weight-light: 300;
    --font-weight-normal: 500;
    --font-weight-bold: 700;
    --font-weight-extra-bold: 800;

    --font-weight-h1: var(--FONT-WEIGHT-BOLD);
    --font-weight-h2: var(--FONT-WEIGHT-BOLD);
    --font-weight-h3: var(--FONT-WEIGHT-BOLD);
    --font-weight-h4: var(--FONT-WEIGHT-BOLD);
    --font-weight-h5: var(--FONT-WEIGHT-BOLD);
    --font-weight-h6: var(--FONT-WEIGHT-BOLD);

    --font-size-base: 1rem;
    --font-size-small: .875rem;
    --font-size-large: 1.125rem;

    --font-size-h1: 2.25rem;
    --font-size-h2: 1.65rem;
    --font-size-h3: 1.4375rem;
    --font-size-h4: 1.3125;
    --font-size-h5: 1.1875;
    --font-size-h6: 1.0625;

    --icon-size-base: 1.25rem;
    --icon-size-small: 1rem;
    --icon-size-large: 1.5rem;

    --line-height-base: 1;
    --line-height-heading: 1.1;
    --line-height-paragraph: 1.3;

    --border-radius: 0;
    --border-width: 1px;

    --animation-duration: 200ms;
    --animation-easing: ease-in-out;
    --animation-normal: var(--ANIMATION-DURATION) var(--ANIMATION-EASING);

    --box-shadow-bottom-left: calc(var(--SPACER) / -2) calc(var(--SPACER) / 2) 0 var(--BOX-SHADOW-COLOR);
    --box-shadow-bottom-right: calc(var(--SPACER) / 2) calc(var(--SPACER) / 2) 0 var(--BOX-SHADOW-COLOR);
    --box-shadow-top-left: calc(var(--SPACER) / -2) calc(var(--SPACER) / -2) 0 var(--BOX-SHADOW-COLOR);
    --box-shadow-top-right: calc(var(--SPACER) / 2) calc(var(--SPACER) / -2) 0 var(--BOX-SHADOW-COLOR);
    --box-shadow: var(--BOX-SHADOW-BOTTOM-RIGHT);

    --readable-content-width: 45rem;
  
    --z-cellar: -1;
    --z-auto: 0;
    --z-base: 1;
    --z-top: 2;
    --z-datepicker: 3;
    --z-flyout: 4;
    --z-tooltip: 5;
    --z-pane-overlay: 6;
    --z-pane: 7;
    --z-statusbar: 8;
    --z-header: 9;
    --z-overlay: 10;
}
</pre>
                <h4 class="u-margin-top-l">Using custom CSS properties</h4>
                <p class="u-margin-top-xs">Whether you are using an overridden value or not, always use the capitalized version of the aforementioned CSS custom properties.</p>
                <pre class="u-margin-top-xs a-pre a-pre--scrollable"><code>.css {
    background-color: var(--BRAND-1);
    color: var(--TEXT-COLOR);
    font-size: var(--FONT-SIZE-BASE);
}</code></pre>
            </div>
        </div>
    </div>
</div>
